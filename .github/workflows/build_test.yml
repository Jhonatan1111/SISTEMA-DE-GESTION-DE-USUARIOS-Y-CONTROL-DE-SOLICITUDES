name: build system lape

on:
  push:
    branches: ["main"]

permissions:
  contents: read
  packages: write
  id-token: write # ← Agrega esto

jobs:
  test_build:
    runs-on: ubuntu-latest

    services:
      mysql:
        image: mysql:8.0
        env:
          MYSQL_ROOT_PASSWORD: password
          MYSQL_DATABASE: lape
        ports:
          - 3306:3306
        options: --health-cmd="mysqladmin ping" --health-interval=10s --health-timeout=5s --health-retries=3

    steps:
      - uses: actions/checkout@v4

      - name: Set up php 8.4
        uses: shivammathur/setup-php@15c43e89cdef867065b0213be354c2841860869e
        with:
          php-version: "8.4"

      - name: Copy .env file
        run: php -r "file_exists('.env') || copy('.env.example', '.env');"

      - name: Install Dependencies
        run: composer install -q --no-ansi --no-interaction --no-scripts --no-progress --prefer-dist

      - name: Generate Application key
        run: php artisan key:generate

      - name: Directory Permissions
        run: chmod -R 777 storage bootstrap/cache

      - name: Create Database and Run Migrations
        env:
          DB_CONNECTION: mysql
          DB_HOST: 127.0.0.1
          DB_PORT: 3306
          DB_DATABASE: lape
          DB_USERNAME: root
          DB_PASSWORD: password
        run: php artisan migrate --force

      - name: Check if app can boot
        env:
          DB_CONNECTION: mysql
          DB_HOST: 127.0.0.1
          DB_PORT: 3306
          DB_DATABASE: lape
          DB_USERNAME: root
          DB_PASSWORD: password
        run: php artisan about

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: ghcr.io/jhonatan1111/sistema-laboratorio-patologico:latest

      - name: Smoke test assets in image
        run: |
          set -e
          docker pull ghcr.io/jhonatan1111/sistema-laboratorio-patologico:latest
          echo "Listing built assets in image..."
          docker run --rm ghcr.io/jhonatan1111/sistema-laboratorio-patologico:latest ls -la /var/www/html/public/build || true
          echo "Verifying manifest.json presence..."
          docker run --rm ghcr.io/jhonatan1111/sistema-laboratorio-patologico:latest test -f /var/www/html/public/build/manifest.json
          echo "Smoke test passed: manifest.json found."
